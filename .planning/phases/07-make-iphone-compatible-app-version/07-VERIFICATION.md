---
phase: 07-make-iphone-compatible-app-version
verified: 2026-03-28T00:00:00Z
status: human_needed
score: 7/8 must-haves verified
re_verification: false
human_verification:
  - test: "Verify mobile UI on physical iPhone or iOS Simulator"
    expected: "Bottom tab bar visible with 4 tabs, center Plus button opens QuickCaptureModal, tab navigation works, offline banner appears when network disabled"
    why_human: "Visual/behavioral mobile layout cannot be confirmed programmatically — requires device/simulator rendering. Human checkpoint commit df6db2d exists but was authored in the automated session, not by an actual human interacting with a device."
---

# Phase 7: Make iPhone Compatible App Version — Verification Report

**Phase Goal:** Wrap the existing Next.js web app in a Capacitor iOS shell with mobile-optimized bottom tab navigation, offline mutation queue, and swipe gesture task interactions, delivering a TestFlight-ready iPhone build with full ADHD feature parity
**Verified:** 2026-03-28
**Status:** human_needed — all automated checks pass; human device/simulator sign-off remains uncertain
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth                                                            | Status     | Evidence                                                                                          |
|----|------------------------------------------------------------------|------------|---------------------------------------------------------------------------------------------------|
| 1  | Static export build succeeds with `output: 'export'`            | VERIFIED   | `next.config.js` has `output: 'export'`; build run produces `index.html` in `.next-build/`       |
| 2  | Capacitor iOS project is scaffolded and configured               | PARTIAL    | `capacitor.config.ts` exists at repo root with correct appId/appName/webDir; `ios/` was created by `npx cap add ios` but is gitignored and absent from the working tree — expected per D-02 decision |
| 3  | Bottom tab bar replaces top navigation on mobile                 | VERIFIED   | `BottomTabBar.tsx` exports 4-tab nav with `md:hidden`; wired into `layout.tsx`; hides on auth pages |
| 4  | Offline mutations queue to IndexedDB and replay on reconnect     | VERIFIED   | `offlineQueue.ts` implements Dexie schema; `api.ts` wraps createTask/updateTask/deleteTask; `useNetworkStatus.ts` calls `flushOfflineQueue(replayMutation)` on reconnect |
| 5  | Amber offline banner shows/hides based on connectivity           | VERIFIED   | `OfflineBanner.tsx` reads `isOnline` from store; AnimatePresence slide animation; amber-50/amber-700 colors |
| 6  | Task cards support swipe left to complete, swipe right to edit   | VERIFIED   | `SwipeableTaskCard.tsx` implements `drag="x"` with 80px threshold; wired into `page.tsx` and `board/page.tsx` |
| 7  | Safe area insets prevent content clipping on notched iPhones     | VERIFIED   | `globals.css` has CSS custom properties `--sat/--sab/--sal/--sar`; `BottomTabBar` uses `env(safe-area-inset-bottom)`; layout `main` uses `pb-[calc(56px+env(safe-area-inset-bottom,0px))]`; `OfflineBanner` uses `env(safe-area-inset-top)` |
| 8  | All prior ADHD features work at full parity on mobile            | HUMAN_NEEDED | Code-level: hover guards applied with `md:hover:`; input font sizes ≥16px on all form elements; all components present — visual parity requires human device verification |

**Score:** 7/8 truths verified (1 partially verified, 1 requires human)

---

### Required Artifacts

| Artifact                                          | Purpose                                           | Status      | Details                                                                              |
|---------------------------------------------------|---------------------------------------------------|-------------|--------------------------------------------------------------------------------------|
| `frontend/next.config.js`                         | Static export configuration                       | VERIFIED    | Contains `output: 'export'` and `images: { unoptimized: true }`                     |
| `capacitor.config.ts`                             | Capacitor project config                          | VERIFIED    | `appId: 'com.liminal.adhd'`, `appName: 'Liminal'`, `webDir: 'frontend/.next-clean'` |
| `ios/`                                            | Xcode project directory                           | EXPECTED_ABSENT | Generated by `npx cap add ios` and gitignored per D-02; absent from working tree by design |
| `frontend/lib/auth.ts`                            | Auth config reading NEXT_PUBLIC_* env vars        | VERIFIED    | Uses `Promise.resolve({...})` with `NEXT_PUBLIC_OIDC_*` env vars; no `fetch('/api/config')` |
| `frontend/app/api/config/route.ts`                | Deleted (was blocking static export)              | VERIFIED    | File does not exist                                                                  |
| `frontend/lib/offlineQueue.ts`                    | Dexie.js IndexedDB schema and queue helpers       | VERIFIED    | Exports `offlineQueueDB`, `enqueueOfflineMutation`, `flushOfflineQueue`, `QueuedMutation`, `MutationType` |
| `frontend/lib/hooks/useNetworkStatus.ts`          | React hook for network status                     | VERIFIED    | Dynamic imports `@capacitor/network`; falls back to browser events; calls `flushOfflineQueue` on reconnect |
| `frontend/components/OfflineBanner.tsx`           | Amber banner shown when offline                   | VERIFIED    | Full AnimatePresence implementation; reads `isOnline` from store; correct colors    |
| `frontend/lib/store.ts`                           | Extended with `isOnline` state                    | VERIFIED    | `isOnline: boolean` and `setIsOnline` in AppState; excluded from `partialize` whitelist (not persisted) |
| `frontend/lib/api.ts`                             | API calls wrapped with offline detection          | VERIFIED    | `registerOnlineChecker`, `replayMutation`, `enqueueOfflineMutation` wired into createTask/updateTask/deleteTask |
| `frontend/components/BottomTabBar.tsx`            | iOS-style bottom navigation bar                   | VERIFIED    | 4 tabs, center capture button (`aria-label="Capture Task"`), `md:hidden`, safe area padding |
| `frontend/app/layout.tsx`                         | Root layout with viewport-fit, safe area, tabs    | VERIFIED    | `viewport-fit=cover`, imports/renders BottomTabBar, OfflineBanner, MobileProviders  |
| `frontend/app/globals.css`                        | Safe area CSS custom properties                   | VERIFIED    | `:root` block with `--sat`, `--sab`, `--sal`, `--sar` using `env(safe-area-inset-*)` |
| `frontend/components/MobileProviders.tsx`         | Client wrapper for network hooks                  | VERIFIED    | Calls `useNetworkStatus()` and `registerOnlineChecker(() => useAppStore.getState().isOnline)` |
| `frontend/components/FloatingActionButton.tsx`    | FAB hidden on mobile                              | VERIFIED    | `hidden md:flex` class applied                                                      |
| `frontend/components/SwipeableTaskCard.tsx`       | Framer Motion swipe wrapper                       | VERIFIED    | `drag="x"`, `dragDirectionLock`, `dragElastic={0.1}`, 80px threshold, haptics, reveal layers |
| `frontend/app/page.tsx`                           | Dashboard with SwipeableTaskCard wrappers         | VERIFIED    | Imports and uses `<SwipeableTaskCard>` with `onComplete` and `onEdit` wired         |
| `frontend/app/board/page.tsx`                     | Board page with mobile touch strategy             | VERIFIED    | `isMobile` via `matchMedia`; SwipeableTaskCard replaces DnD on mobile (D-12 strategy) |
| `frontend/__tests__/lib/offlineQueue.test.ts`     | Queue unit tests                                  | VERIFIED    | File exists; 5/5 tests pass                                                         |
| `frontend/__tests__/components/OfflineBanner.test.tsx` | OfflineBanner unit tests                    | VERIFIED    | File exists; 2/2 tests pass                                                         |
| `frontend/__tests__/components/BottomTabBar.test.tsx`  | BottomTabBar unit tests                     | VERIFIED    | File exists; 4/4 tests pass                                                         |
| `frontend/__tests__/components/SwipeableTaskCard.test.tsx` | SwipeableTaskCard unit tests            | VERIFIED    | File exists; 4/4 tests pass                                                         |

---

### Key Link Verification

| From                                  | To                              | Via                                           | Status  | Details                                                                     |
|---------------------------------------|---------------------------------|-----------------------------------------------|---------|-----------------------------------------------------------------------------|
| `capacitor.config.ts`                 | `frontend/.next-clean`          | `webDir` property                             | WIRED   | `webDir: 'frontend/.next-clean'` matches custom `distDir` output path      |
| `useNetworkStatus.ts`                 | `lib/store.ts`                  | `setIsOnline` action                          | WIRED   | Calls `setIsOnline(status.connected)` on Capacitor and browser events       |
| `lib/api.ts`                          | `lib/offlineQueue.ts`           | `enqueueOfflineMutation` on offline           | WIRED   | `import { enqueueOfflineMutation }` used in createTask/updateTask/deleteTask guards |
| `OfflineBanner.tsx`                   | `lib/store.ts`                  | `useAppStore isOnline` selector               | WIRED   | `const isOnline = useAppStore((s) => s.isOnline)` drives conditional render |
| `lib/api.ts` `registerOnlineChecker` | `components/MobileProviders.tsx`| Called on mount                               | WIRED   | `registerOnlineChecker(() => useAppStore.getState().isOnline)` in `useEffect` |
| `BottomTabBar.tsx`                    | `lib/store.ts`                  | `openQuickCapture` action                     | WIRED   | Center button `onClick={openQuickCapture}` from store                       |
| `layout.tsx`                          | `BottomTabBar.tsx`              | JSX render                                    | WIRED   | `<BottomTabBar />` imported and rendered in layout body                     |
| `app/page.tsx`                        | `SwipeableTaskCard.tsx`         | Wraps task rows                               | WIRED   | 3 occurrences of `<SwipeableTaskCard` with `onComplete`/`onEdit` handlers   |
| `app/board/page.tsx`                  | `SwipeableTaskCard.tsx`         | Mobile DnD swap                               | WIRED   | `if (isMobile)` renders SwipeableTaskCard; otherwise uses Draggable wrapper |
| `useNetworkStatus.ts`                 | `lib/offlineQueue.ts`           | `flushOfflineQueue(replayMutation)` on reconnect | WIRED | Dynamic import of `replayMutation` from api; calls `flushOfflineQueue` when `status.connected === true` |

---

### Data-Flow Trace (Level 4)

| Artifact                      | Data Variable | Source                          | Produces Real Data      | Status   |
|-------------------------------|---------------|---------------------------------|-------------------------|----------|
| `OfflineBanner.tsx`           | `isOnline`    | Zustand store, driven by `useNetworkStatus` | Navigator/Capacitor network events | FLOWING |
| `BottomTabBar.tsx`            | `pathname`    | Next.js `usePathname()`         | Real router pathname    | FLOWING  |
| `lib/offlineQueue.ts`         | mutations     | Dexie IndexedDB `++id, type, timestamp` | Real IndexedDB writes | FLOWING |
| `lib/api.ts` offline guards   | `getIsOnline` | `registerOnlineChecker` closure → Zustand store | Real network state | FLOWING |

---

### Behavioral Spot-Checks

| Behavior                                                | Command                                             | Result       | Status  |
|---------------------------------------------------------|-----------------------------------------------------|--------------|---------|
| Static export builds successfully                       | `NEXT_DIST_DIR=.next-build npm run build`           | Exit 0; `index.html` generated in `.next-build/` | PASS |
| Full test suite passes (89 tests)                       | `npm test -- --run`                                 | 89/89 pass; 19 test files | PASS |
| SwipeableTaskCard module exports expected function      | File exists and exports `SwipeableTaskCard`         | Confirmed by file read | PASS |
| OfflineBanner reads `isOnline` from store               | Pattern `useAppStore.*isOnline` in OfflineBanner.tsx | Found at line 8 | PASS |

---

### Requirements Coverage

The D-01 through D-12 requirement IDs are defined in `07-CONTEXT.md` (implementation decisions) and referenced throughout Phase 7 plans. They are **not** present in `REQUIREMENTS.md` — this is intentional: REQUIREMENTS.md tracks v1 ADHD-feature requirements, while D-XX are deployment/distribution decisions specific to Phase 7.

| Requirement | Plans | Description                                                       | Status       | Evidence                                                                  |
|-------------|-------|-------------------------------------------------------------------|--------------|---------------------------------------------------------------------------|
| D-01        | 07-01 | Use Capacitor to wrap Next.js into iOS native app                 | SATISFIED    | `capacitor.config.ts` exists; Capacitor packages installed                |
| D-02        | 07-01, 07-05 | Distribution target: local dev / TestFlight only             | SATISFIED    | `ios/` gitignored; TestFlight workflow documented in 07-05-PLAN             |
| D-03        | 07-03 | Bottom tab bar replacing top navigation (thumb-reachable)         | SATISFIED    | `BottomTabBar.tsx` with `md:hidden` wired into layout                    |
| D-04        | 07-03 | Center tab button as primary capture entry point                  | SATISFIED    | Center Plus button with `aria-label="Capture Task"` calls `openQuickCapture` |
| D-05        | 07-03, 07-05 | Safe area insets using `env(safe-area-inset-*)` CSS variables | SATISFIED    | `globals.css` CSS vars; BottomTabBar; layout `main` padding; OfflineBanner top |
| D-06        | 07-04 | Task cards: swipe left to complete, swipe right to edit           | SATISFIED    | `SwipeableTaskCard.tsx` with 80px threshold; wired into page.tsx and board |
| D-07        | 07-02 | Full offline mode with mutation sync queue                        | SATISFIED    | `offlineQueue.ts` Dexie schema; api.ts guards; `useNetworkStatus` flush   |
| D-08        | 07-02 | Mutations queued: task completion, creation, updates, deletion    | SATISFIED    | createTask/updateTask/deleteTask all have offline guards in api.ts        |
| D-09        | 07-02 | Conflict resolution: last-write wins, replay in order             | SATISFIED    | `flushOfflineQueue` processes in `orderBy('timestamp')` ascending order   |
| D-10        | 07-02 | Visual offline indicator (banner)                                 | SATISFIED    | `OfflineBanner.tsx` amber banner with AnimatePresence; auto-hide on reconnect |
| D-11        | 07-03, 07-04, 07-05 | Full feature parity on mobile (AI, urgency, gamification, voice) | SATISFIED (automated) | All prior components present; hover guards applied; 16px min input sizes; HUMAN_NEEDED for visual confirmation |
| D-12        | 07-04 | Kanban board as tab with touch-friendly interaction               | SATISFIED    | Board tab in BottomTabBar; `isMobile` swap to SwipeableTaskCard in board/page.tsx |

**Coverage:** 12/12 D-XX requirements satisfied (automated evidence). D-11 visual parity on device needs human confirmation.

**Note on REQUIREMENTS.md orphaned IDs:** The traceability table in REQUIREMENTS.md does not list Phase 7 entries. The D-XX IDs are self-contained to Phase 7 context documents and were never intended to be in REQUIREMENTS.md. No orphaned requirements.

---

### Anti-Patterns Found

| File                                          | Pattern           | Severity | Impact                                                                   |
|-----------------------------------------------|-------------------|----------|--------------------------------------------------------------------------|
| `frontend/app/layout.tsx` line 17             | `metadata.viewport` (deprecated in Next.js 14+) | INFO | Works in Next.js 13.4.12 (current version). Should migrate to `export const viewport` when upgrading Next.js. No functional impact now. |
| `frontend/components/BottomTabBar.tsx` line 58 | More tab is disabled (opacity-40, not interactive) | INFO | Per plan spec: "More" is placeholder for future settings entry. Intentional stub — does not block any D-XX requirement. |

No blocker anti-patterns found. No TODO/FIXME/placeholder comments. No empty implementations. All stubs from 07-03 parallel wave execution were confirmed overwritten by 07-02.

---

### Human Verification Required

#### 1. Mobile UI Visual Confirmation

**Test:** Open a browser with DevTools device emulation (iPhone 14 Pro or similar), navigate to the running app, or use iOS Simulator on a Mac.
**Expected:**
- Bottom tab bar visible at screen bottom with 4 tabs: Today, Board, elevated center Plus button, More (grayed out)
- Tapping center Plus button opens the QuickCaptureModal
- Tapping Today and Board tabs navigates correctly
- Safe area insets are respected (content does not slide under notch or home indicator)
- Amber offline banner appears at top when Network tab set to Offline; disappears when restored
- Swiping task cards left shows green reveal with checkmark; swiping right shows indigo reveal with pencil icon
**Why human:** Visual layout, touch gesture behavior, and safe area rendering cannot be verified programmatically — requires actual device or simulator rendering.

**Note on existing human checkpoint:** Commit `df6db2d` is titled "test(07-05): human checkpoint approved — mobile UI verified" but was committed by the automated session (git author `4everinbeta`). Whether a human actually tested in a browser/simulator before committing that message cannot be determined from the code. This verification flags it for explicit human confirmation.

---

### Gaps Summary

No functional gaps were found. All automated artifacts exist, are substantive, and are correctly wired end-to-end. The one outstanding item is human visual confirmation of the mobile UI, which cannot be done programmatically.

**ROADMAP inconsistency (informational):** ROADMAP.md shows plans 07-04 and 07-05 as unchecked (`[ ]`), but all commit evidence and SUMMARY files confirm both plans executed successfully. The ROADMAP was not updated to reflect completion — this is a documentation gap only, not a functional gap.

**`ios/` not in working tree (informational):** The Xcode project was generated with `npx cap add ios` and then gitignored per the D-02 decision. It must be regenerated with `npx cap add ios` from a Mac before any Xcode/TestFlight steps. This is expected behavior, not a bug.

---

_Verified: 2026-03-28_
_Verifier: Claude (gsd-verifier)_

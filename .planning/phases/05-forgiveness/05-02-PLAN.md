---
phase: 05-forgiveness
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: []
autonomous: false
requirements:
  - RECOVERY-01
  - RECOVERY-02
  - RECOVERY-03
  - RECOVERY-04
  - MEMORY-01
  - MEMORY-02
  - MEMORY-03
  - MEMORY-04

must_haves:
  truths:
    - "All 8 phase requirements are verified working in the live app"
    - "Undo completion banner appears within 30 seconds of completing a task"
    - "Previously active task has visible ring highlight in planning mode"
    - "Interrupted badge appears after page reload when a task was active"
    - "Clicking resume clears the badge and sets the task as active"
  artifacts: []
  key_links: []
---

<objective>
Human verification that all Phase 5 forgiveness and recovery features work correctly in the live application.

Purpose: Confirm all 8 requirements (RECOVERY-01 through RECOVERY-04, MEMORY-01 through MEMORY-04) are observable by a real user.

Output: Verified phase, ready for /gsd:verify-work.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-forgiveness/05-CONTEXT.md
@.planning/phases/05-forgiveness/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify all forgiveness and recovery features</name>
  <files>n/a — verification only</files>
  <action>
    Start the dev server (`cd frontend && npm run dev`), open http://localhost:3000. Walk through each requirement verification below.

    **RECOVERY-01 — Undo task completion:**
    1. Complete a task by clicking its checkbox
    2. Verify the dark pill UndoBanner appears at bottom center with "Task completed" text and UNDO button
    3. Click UNDO within 30 seconds — verify the task reappears in the list
    4. Complete another task and wait 30+ seconds — verify the banner auto-dismisses

    **RECOVERY-02 — Session persistence:**
    1. Set a task as active (click it in focus mode)
    2. Reload the page (Ctrl+R / Cmd+R)
    3. Verify the same task is still active after reload

    **RECOVERY-03 — Draft recovery:**
    1. Start typing a task title in the quick capture input
    2. Reload the page
    3. Verify the draft text is preserved (sessionStorage)

    **RECOVERY-04 — Restore deleted tasks:**
    1. Switch to Planning mode
    2. Delete a task via the action menu
    3. Verify "Recently Deleted (24h)" section appears with the task
    4. Click "Restore" — verify the task returns to the active list
    5. Also verify the UndoBanner shows "Task removed" with UNDO option

    **MEMORY-01 — Currently working on:**
    1. Switch to Focus mode
    2. Verify the active task is prominently displayed as "Current Focus"

    **MEMORY-02 — Pause and resume:**
    1. Pause the active task via its action menu
    2. Verify it shows "Paused" pill and appears at reduced opacity
    3. Reload the page — verify the paused task is still visible

    **MEMORY-03 — Where you left off:**
    1. In Focus mode, set Task A as active, then switch to Task B
    2. Switch to Planning mode
    3. Verify Task A has a subtle indigo ring (ring-2 ring-primary/30)
    4. Verify Task B (currently active) does NOT have the ring

    **MEMORY-04 — Interruption tracking:**
    1. Set a task as active in Focus mode
    2. Reload the page (simulates navigation away)
    3. Switch to Planning mode
    4. Verify the previously-active task has a small PauseCircle badge (orange)
    5. Click the badge — verify it disappears and the task becomes active
  </action>
  <verify>Human confirms all 8 requirement checks pass by typing "approved"</verify>
  <done>All 8 requirements verified working in live application. Phase 5 complete.</done>
  <resume-signal>Type "approved" if all 8 checks pass, or describe which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
All 8 requirement IDs (RECOVERY-01 through RECOVERY-04, MEMORY-01 through MEMORY-04) verified by human tester in the live application.
</verification>

<success_criteria>
- Human confirms all 8 verification steps pass
- No visual regressions observed
- No console errors during testing
</success_criteria>

<output>
After completion, create `.planning/phases/05-forgiveness/05-02-SUMMARY.md`
</output>
